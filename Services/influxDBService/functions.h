#ifndef FUNCTIONS_H
#define FUNCTIONS_H

#include <string>
#include <functional>

std::string exampleFunction(const std::string&);
std::string executeFluxQuery(const std::string& body);

#endif // FUNCTIONS_H
